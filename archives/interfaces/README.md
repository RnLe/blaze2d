# Historical interface inputs

These examples use configuration dialects retired in release 0.7. They are preserved
as historical inputs and are not accepted by the current parser. Runnable configurations
are in `examples/calculations`; the website reference documents migration to `blaze2d/1`.
